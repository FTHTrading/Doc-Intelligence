# ─────────────────────────────────────────────────────────────
# FTH Sovereign Infrastructure — Environment Configuration
#
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to source control.
# ─────────────────────────────────────────────────────────────

# ── Telnyx (Sovereign Comms Agent) ────────────────────────────
TELNYX_API_KEY=
TELNYX_WEBHOOK_SECRET=
TELNYX_MESSAGING_PROFILE=

# ── Cloudflare Tunnel ─────────────────────────────────────────
# Get this from: cloudflared tunnel create fth-infra
CLOUDFLARE_TUNNEL_TOKEN=

# ── Backup Configuration ─────────────────────────────────────
# Interval between automated backup snapshots (minutes)
FTH_BACKUP_INTERVAL_MINUTES=15

# AES-256 encryption key for backup archives
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FTH_BACKUP_ENCRYPTION_KEY=

# How many days to retain backup snapshots
FTH_BACKUP_RETENTION_DAYS=30

# ── IPFS (Optional — Private Swarm Key) ──────────────────────
# If running a private IPFS swarm, set the swarm key
IPFS_SWARM_KEY=
